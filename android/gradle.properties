org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=false
# Force Gradle to use the system JDK (Temurin 17) to avoid Android Studio JBR (Java 21) incompatibilities
# Update this path if you installed a different JDK location on your system.
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.16.8-hotspot

# Suppress compileSdk warning until AGP is upgraded to a version that supports compileSdk=36
android.suppressUnsupportedCompileSdk=36

# Skip Flutter's build dependency validation to ignore AGP/Kotlin version warnings
android.skip-build-dependency-validation=true
